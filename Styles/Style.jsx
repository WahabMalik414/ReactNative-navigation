import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  text: {
    textAlign: 'center',
    fontSize: 50,
    fontWeight: 'bold',
    marginTop: 200,
  },
});

export default styles;
